name: Keep Alive

on:
  schedule:
    - cron: "0 12 * * *"   # noon UTC daily

jobs:
  ping:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Ping Streamlit app
        run: curl -s -o /dev/null -w "%{http_code}" https://nyc-311-service-equity.streamlit.app/key_findings
